package authz

// session service
// which will return id of a token
// store it in a map with it's validity
// and then blacklist by token id with expiration time

// it also optionally could cache user info
